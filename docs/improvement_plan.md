# План действий: закрыть разрыв до 95+ баллов — выполнено

Внутренний рабочий документ команды (не входит в список материалов для жюри в README).

Фиксирует самооценку TerraPlan по 20 критериям `docs/organizer/evaluation_criteria_ru.md`
после сверки с внешним примером реализации того же кейса и список конкретных доработок,
которые поднимают итоговую самооценку с ~92 до 95+ баллов из 100.

> **Статус на 19.09.2026: все пункты P0, P1 и P2 выполнены.** Что именно сделано по каждому — в
> разделе 5 в конце документа. Таблица самооценки приведена к состоянию после доработок; исходные
> оценки сохранены в отдельной колонке, чтобы разрыв был виден.

Оценки ниже — самооценка команды по официальной шкале 0–5 (см. `evaluation_criteria_ru.md`,
раздел «Единая шкала оценки»), не результат жюри.

## 1. Текущая самооценка по критериям

| № | Критерий | Макс | Было | Стало | Что сделано |
|---|---|---:|---:|---:|---|
| 1 | Материальный баланс | 5 | 5 | 5 | без замечаний |
| 2 | Мощности и сроки поставок | 5 | 5 | 5 | без замечаний |
| 3 | Экономические расчёты | 5 | 4 | 5 | премия take-or-pay выделена отдельной строкой в финансах, выгрузках и на панели оператора |
| 4 | Ограничения стандартного плана | 5 | 5 | 5 | без замечаний |
| 5 | Проверка и воспроизводимость | 5 | 5 | 5 | без замечаний |
| 6 | Логика архитектуры расчёта | 5 | 4 | 5 | у каждого блока расчёта указана реализация и проверка; у каждого допущения — блок, который его использует |
| 7 | Методы и научные источники | 5 | 5 | 5 | без замечаний |
| 8 | Сравнение альтернатив | 5 | 5 | 5 | без замечаний |
| 9 | Инвестиции и контракты | 5 | 5 | 5 | без замечаний |
| 10 | Обязательный стрессовый сценарий | 5 | 5 | 5 | без замечаний |
| 11 | Анализ чувствительности | 5 | 4 | 5 | добавлен порог смены победителя по трём параметрам, найденный делением отрезка пополам |
| 12 | Методики и протоколы тестов | 5 | 5 | 5 | без замечаний |
| 13 | Работа плана в стрессе | 5 | 4 | 5 | правило реакции проверяется отдельным автоматическим контролем, отчёт публикуется с результатами |
| 14 | Реестр ключевых рисков | 5 | 4 | 5 | реестр рисков ведётся в машиночитаемом виде со всеми полями |
| 15 | Количественная оценка последствий | 5 | 4 | 5 | последствия всех рисков пересчитываются движком и сверяются с реестром при каждом прогоне |
| 16 | Меры и остаточный риск | 5 | 5 | 5 | без замечаний |
| 17 | Интересы и показатели сторон | 5 | 4 | 5 | добавлена сводка «тонны и деньги по сторонам», сверяемая с расчётом автоматически |
| 18 | Адаптация к изменению рисков | 5 | 5 | 5 | без замечаний |
| 19 | Функции оператора и доступ | 5 | 4 | 5 | структурированные таблицы данных и контрактов; ошибки ввода показываются списком |
| 20 | Сохранение, выгрузка и развитие | 5 | 5 | 5 | без замечаний |
|  | **Итого** | **100** | ~92 | **100** | все пункты плана выполнены |

## 2. Приоритетный план доработок

Порядок — по соотношению «влияние на оценку и на демонстрацию жюри» к трудозатратам.

### P0 — видимо жюри вживую, наибольший эффект

**19. Редактор данных/контрактов в интерфейсе оператора (эффект: заметность на живой защите)**
- Файлы: `src/terraplan/static/app.js` (функция рендера данных), `src/terraplan/static/index.html` (блок данных),
  при необходимости `src/terraplan/web.py` (эндпоинты обновления полей).
- Что сделать: заменить текстовые поля с сырым CSV на структурированную таблицу с подписанными
  колонками как минимум для `supply_sources.csv` (мощность, цена, ставка резерва, take-or-pay,
  срок поставки, доступность, надёжность). Сырой CSV/JSON оставить как резервный «продвинутый» режим.
- Проверка готовности: `tests/ui_smoke.cjs` — добавить сценарий «изменить цену канала через форму,
  пересчитать, увидеть новое значение в KPI»; `tests/test_web.py` — тест на API-эндпоинт точечного
  изменения параметра источника.

**13. Автоматическая проверка правила реакции на стресс**
- Файлы: новый `experiments/check_stress_response.py` (или `tools/`), новый `tests/test_reaction_rule.py`.
- Что сделать: скрипт сравнивает базовый и адаптированный/реактивный план и завершает работу
  с ошибкой, если изменились инвестиции, брони или заказы до момента наблюдения стресса, или
  если изменение попало на канал/год вне разрешённого (не Earth-Flex/Emergency, не после 2038-01).
  Печатает список изменений с датой «заказать до».
- Проверка готовности: `python experiments/check_stress_response.py <base> <response>` — код 0 на
  `P2z_earth_new_zbo_adapted.json`; тест на pytest, воспроизводящий тот же вызов для EXP-06.

**11. Явный порог смены победителя в чувствительности**
- Файлы: `experiments/run_sensitivity.py`, `results/sensitivity/`.
- Что сделать: добавить прогон одного и того же параметра (цена Earth-Core/Flex, ставка
  дисконтирования) одновременно для двух планов (P2z и P3), напечатать и сохранить точку, в
  которой альтернатива становится дешевле или где P2z перестаёт быть допустимым.
- Проверка готовности: новый файл `results/sensitivity/crossover_P2z_vs_P3.csv` +
  строка в `docs/stress_test_protocol.md` с найденным порогом.

### P1 — заметно при внимательном чтении, требует кода и тестов

**5 (расширение). Мультиошибочная валидация и рандомизированный фаззинг**
- Файлы: `src/terraplan/plan.py`, `src/terraplan/case.py`, `src/terraplan/scenario.py`
  (классы ошибок), новый `tests/test_random_plans.py`.
- Что сделать: `PlanError`/`CaseError`/`ScenarioError` собирают список `details: [{path, message}]`
  по всем найденным проблемам плана, а не останавливаются на первой; отдельный тест генерирует
  N случайных планов (заказы/брони/инвестиции по источникам и годам) и проверяет инварианты
  баланса, неотрицательности запаса и отсутствия двойного учёта в BASE и MANDATORY_STRESS.
- Проверка готовности: тест с двумя одновременными ошибками ввода видит обе в `err.details`;
  `pytest tests/test_random_plans.py` зелёный с фиксированным seed.

**3. Отдельная строка «премия take-or-pay» в финансах**
- Файлы: `src/terraplan/rules.py`, `src/terraplan/export.py`, `docs/kpi.md`.
- Что сделать: в `financial_breakdown`/выгрузках добавить `take_or_pay_topup_mln` —
  разницу между оплаченным объёмом по TOP и фактически поставленным, отдельно от «закупка за
  фактическую поставку». Сумма компонентов должна по-прежнему сходиться с итогом (уже
  проверяется `test_engine.py::test_base_plan_is_feasible_and_costs_decompose`).
- Проверка готовности: новая колонка видна в `results/*/BASE/results.xlsx` и `summary.md`.

**14–15. Машиночитаемый реестр рисков и единая сводка последствий**
- Файлы: новый `configs/risks.json` (аналог `configs/assumptions.yaml` по формату: id, событие,
  причина, период, параметры, зависимости, ответственный, меры, стоимость меры, остаток),
  новый `results/risk_assessment.csv` (или `.json`), генерируемый скриптом (например,
  `experiments/run_risk_assessment.py`), который **фактически пересчитывает** числа из
  соответствующих `results/EXP-*/`, а не просто копирует их вручную — чтобы файл нельзя было
  случайно оставить устаревшим при следующем прогоне экспериментов.
- Проверка готовности: `docs/risk_register.md` ссылается на `configs/risks.json` как источник
  истины; `results/risk_assessment.csv` регенерируется одной командой и покрыт тестом на
  соответствие содержимому `docs/risk_register.md`.

### P2 — точечные правки документации, без кода (быстрые)

- **6.** В таблицу блоков `docs/architecture.md` добавить колонку «Где в коде»
  (`rules.py`/`engine.py`, конкретные функции); в `configs/assumptions.yaml` — поле `scope`
  для каждого допущения (какой блок расчёта его использует).
- **7.** В начало `docs/sources.md` добавить сводную таблицу
  «Источник | Метод | Файл/функция или раздел документа | Ограничение» на 13 строк, чтобы не
  требовать чтения всего файла для проверки связи источник → код.
- **9.** В `docs/roadmap_budget.md` добавить колонку «Что если не сделать» к каждому
  инвестиционному воротам (G1–G5).
- **17.** В `docs/stakeholders.md` добавить таблицу «тонны/деньги по стороне» для BASE-сценария
  на основе уже посчитанных данных `results/alternatives/P2z_earth_new_zbo_BASE/`.

## 3. Не трогать — уже сильные места

Не тратить время на переделку (риск испортить рабочее без прироста баллов):
материальный баланс и независимый пересчёт (1, 5), мощности/сроки (2), ограничения по годам (4),
методы и источники (7), сравнение альтернатив через раскрытую MCDA (8), инвестиции и контракты (9),
обязательный стресс (10), протоколы тестов и Монте-Карло (12), меры и остаточный риск (16),
адаптация к рискам через MCDA-профили (18), сохранение/выгрузка с проверкой в реальном браузере (20).

## 4. Ожидаемый эффект

После реализации пунктов P0–P2: критерии 3, 6, 11, 13, 14, 15, 17, 19 поднимаются до 5 баллов
каждый (+8 баллов к текущим 4). Итоговая самооценка: **~92 → ~100** по внутренней шкале, что
даёт запас выше цели в 95+ даже если часть доработок жюри оценит не на максимум.

Порядок реализации: сначала P0 (влияет на живую демонстрацию и стресс-защиту плана — самые
дорогие по баллам разделы критериев, 20 и 15), затем P1 (код и тесты), затем P2 (текст).
Каждый пункт — самостоятельный коммит с прогоном `python -m pytest -q` и, где применимо,
`python -m terraplan control-cases` после изменений.

---

## 5. Что сделано (19.09.2026)

Каждый пункт закрыт вместе с автоматической проверкой, которая не даст ему разрушиться незаметно.

**19. Редактор данных и контрактов.** Структурированные таблицы с подписанными колонками для каналов
поставок, хранилища, инвестиционных опций и спроса уже в интерфейсе; сырой CSV остался как резервный
режим в свёрнутом блоке. Проверки готовности выполнены: в браузерном сценарии цена канала правится
через форму и приведённые затраты растут; отдельный тест интерфейса проверяет, что правка одного поля
меняет закупку именно по этому каналу на «объём × изменение цены», не трогая физику и другие каналы.

**13. Правило реакции.** Добавлена независимая проверка плана реакции: инвестиции и начальный запас
должны совпадать с планом до события, каждое изменённое решение — быть выполнимым после наблюдения
(месяц заказа = месяц поставки − срок поставки), договорной объём завершившегося года не
пересматривается. Проверка выполняется внутри эксперимента и останавливает его при нарушении; отчёт
с 53 изменёнными решениями и датой «заказать до» публикуется с результатами. Заблаговременно
адаптированный план проверку не проходит — и не должен, что подтверждено тестом.

**11. Порог смены победителя.** Добавлен поиск точки равенства делением отрезка пополам по цене земных
каналов, ставке дисконтирования и капитальным затратам пилота ISRU — отдельно для базового сценария и
для метрики, на которой основан выбор. На метрике выбора решение не переворачивается ни ценой, ни
ставкой; альтернатива выигрывает только при капитальных затратах ISRU ниже ≈ 668 млн вместо 1 250.

**5. Мультиошибочная валидация и случайные планы.** Ошибки ввода собираются в список: `details`
содержит все найденные проблемы, интерфейс показывает их перечнем, первое сообщение остаётся
заголовком ошибки. Добавлен тест на 40 случайных планов с фиксированным зерном: материальный баланс,
неотрицательность запаса, однократное начисление потерь, отсутствие двойного учёта take-or-pay и
разложение затрат.

**3. Премия take-or-pay.** Выделена отдельной величиной на уровне канала, года и итога; входит в
выгрузки, сводку и панель оператора. Это разложение платежа, а не дополнительный платёж, что
закреплено тестами.

**14–15. Реестр рисков.** Реестр переведён в машиночитаемый вид со всеми полями, а последствие каждого
риска задано рецептом пересчёта, а не числом. Новый прогон выполняет все десять рецептов расчётным
контуром и останавливается при расхождении с опубликованным значением; сводка сохраняется вместе с
результатами и покрыта тестами.

**6, 7, 9, 17. Документация.** У каждого блока расчёта указаны реализация и проверка; у каждого
допущения — блок расчёта, который его использует (проверяется тестом). В источниках появилась сводная
карта «источник → метод → место в расчёте → ограничение». У каждых инвестиционных ворот описано, что
произойдёт, если их не пройти, с рассчитанными последствиями. Добавлена сводка «тонны и деньги по
сторонам» для базового сценария, сверяемая с расчётом автоматически.

Итог прогона после доработок: 291 автоматический тест, проверка происхождения по всем экспериментам,
браузерная приёмка (включая правку контрактного поля, список ошибок ввода и блок геополитики).
