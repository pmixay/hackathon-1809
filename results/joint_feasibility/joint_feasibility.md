# EXP-15. Существует ли единый фиксированный график для BASE и обязательного стресса

## Доказательство (необходимое условие, без перебора)

Для одного и того же графика заказов `x` в обоих сценариях одновременно требуются

* ёмкость хранилища в BASE на конец декабря года `y-1`: `N(<y) <= C + SB(<y) - I0`;
* резерв 45 дней в стрессе на 1 января года `y`: `N_sigma(<y) >= R(y) + SS(<y) - I0`,

где `N` — нетто-поступление, `N_sigma <= N` — то же при фактических долях поставки стресса,
`SB`/`SS` — фактически выданные объёмы, `C` — ёмкость, `I0` — начальный запас. Отсюда

```
R(y) + SS(<y) <= C + SB(<y) <= C + D_base(<y)
```

Условие не содержит `x`: при его нарушении единого графика не существует ни при каком
распределении заказов по месяцам и каналам. Недовыдача в BASE условие только ужесточает,
поэтому проверка при полном обслуживании BASE — самая благоприятная для существования графика.

| Год y | R(y), т | Доп. спрос стресса до y, т | Ёмкость C, т | Требуется R+SS, т | Допускается C+D_base, т | Разрыв, т | Выполнено |
|---|---:|---:|---:|---:|---:|---:|:---:|
| 2036 | 17.260 | 0.0 | 120 | 117.260 | 220.000 | -102.740 | да |
| 2037 | 23.425 | 0.0 | 120 | 263.425 | 360.000 | -96.575 | да |
| 2038 | 35.445 | 0.0 | 120 | 465.445 | 550.000 | -84.555 | да |
| 2039 | 45.370 | 37.5 | 120 | 762.870 | 800.000 | -37.130 | да |
| 2040 | 55.295 | 85.5 | 120 | 1140.795 | 1120.000 | +20.795 | **НЕТ** |

**Вывод.** Необходимое условие нарушено для 2040 года с разрывом **20.795 т**. Единого фиксированного графика, который полностью обслуживает оба сценария и одновременно удерживает ёмкость хранилища в BASE и 45-дневный резерв в стрессе на 1 января 2040 г., **не существует**. Это следует из данных кейса, а не из непригодности какого-то одного графика.

Причина видна из самой формулы: к 2040 г. стресс требует выдать на **85.5 т** больше BASE, и ровно этот излишек в BASE остаётся в хранилище, ёмкость которого — 120 т, тогда как резерв стресса требует к тому же моменту ещё 55.295 т.

**Точная граница.** Единый график арифметически возможен только ценой недовыдачи в стрессе: обслуженный объём стресса до 2040 г. не может превышать **0.9808** его спроса (1064.7 т из 1085.5 т), то есть требует согласиться минимум на 20.8 т накопленного дефицита. Раздельные сценарные планы этой платы не требуют.

## Перебор (подтверждение, не доказательство)

Проверено семейство единых графиков — интерполяция между планом BASE и стресс-адаптированным планом (11 вариантов). Проходят оба сценария: **0**. Подробности — `search.md`.

| Вариант | BASE | Стресс | Жёсткие нарушения BASE | Жёсткие нарушения стресса |
|---|:---:|:---:|---|---|
| lambda=0.0 | исполним | нет | — | RESERVE_45D |
| lambda=0.1 | исполним | нет | — | RESERVE_45D |
| lambda=0.2 | исполним | нет | — | RESERVE_45D |
| lambda=0.3 | исполним | нет | — | RESERVE_45D |
| lambda=0.4 | нет | нет | INTRA_MONTH_PEAK | RESERVE_45D |
| lambda=0.5 | нет | нет | INTRA_MONTH_PEAK | RESERVE_45D |
| lambda=0.6 | нет | нет | INTRA_MONTH_PEAK; STORAGE_OVERFLOW | RESERVE_45D |
| lambda=0.7 | нет | нет | INTRA_MONTH_PEAK; STORAGE_OVERFLOW | RESERVE_45D |
| lambda=0.8 | нет | нет | INTRA_MONTH_PEAK; STORAGE_OVERFLOW | RESERVE_45D |
| lambda=0.9 | нет | нет | INTRA_MONTH_PEAK; STORAGE_OVERFLOW | RESERVE_45D |
| lambda=1.0 | нет | исполним | INTRA_MONTH_PEAK; STORAGE_OVERFLOW | — |

Перебор согласуется с доказательством: ни один единый график не проходит оба сценария. Сам по себе перебор несуществования не доказывает — доказательство даёт условие выше.

